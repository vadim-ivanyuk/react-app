import styled from 'styled-components';

import { red } from '../../../theme';

export const CourseAuthors = styled.div`
	width: 100%;
	margin-top: 20px;
`;

export const Error = styled.p`
	font-size: 14px;
	color: ${red};
	padding-left: 5px;
`;
